# EduCompare Project

This project organizes research and comparison data for education systems in Taiwan and Thailand.

## Structure

- `docs/` contains planning and project documentation.
- `data/raw/` stores source data by country.
- `data/processed/` stores cleaned or transformed datasets.
- `data/templates/` stores reusable templates for data collection and analysis.
